#include <iostream>
#include <math.h>
#include <vector>
#include <numeric>

using namespace std;

int wip_abc104_c() {

    int D, G;
    cin >> D >> G;

    vector<int> P(D);
    vector<int> C(D);

    for (int i = 0; i < D; ++i) {
        cin >> P[i];
        cin >> C[i];
    }

    return 0;
}